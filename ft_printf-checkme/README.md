# ft_printf


## 📖 Описание

**ft_printf** — это собственная реализация стандартной функции `printf()` из библиотеки C. Проект направлен на глубокое понимание вариадических функций, парсинга форматных строк и работы с различными типами данных.


### Цели проекта:

- Понимание работы вариадических функций (variadic functions)
- Освоение парсинга сложных форматных строк
- Реализация конвертации различных типов данных
- Работа с флагами форматирования (bonus часть)


---

## ⚙️ Инструкции


### Требования

- Unix/Linux система
- GCC компилятор
- Make


### Компиляция

```bash
# Клонировать репозиторий
git clone [ваш репозиторий]
cd ft_printf

# Компиляция обязательной части
make

# Компиляция с bonus
make bonus

# Очистка объектных файлов
make clean

# Полная очистка
make fclean

# Перекомпиляция
make re
```

### Использование в вашем проекте

```c
#include "ft_printf.h"

int main(void)
{
    ft_printf("Hello, %s!\n", "World");
    ft_printf("Number: %d, Hex: %x\n", 42, 255);
    return (0);
}
```

**Компиляция**:
```bash
gcc -Wall -Wextra -Werror main.c -L. -lftprintf -o program
./program
```

---

## 🎯 Функциональность

### Обязательная часть

| Спецификатор | Описание | Пример |
|--------------|----------|---------|
| `%c` | Печать одного символа | `ft_printf("%c", 'A')` → `A` |
| `%s` | Печать строки | `ft_printf("%s", "Hello")` → `Hello` |
| `%p` | Печать указателя | `ft_printf("%p", ptr)` → `0x7ffc8b4a` |
| `%d` / `%i` | Печать знакового числа | `ft_printf("%d", -42)` → `-42` |
| `%u` | Печать беззнакового числа | `ft_printf("%u", 4294967295)` → `4294967295` |
| `%x` | Печать hex (lowercase) | `ft_printf("%x", 255)` → `ff` |
| `%X` | Печать hex (UPPERCASE) | `ft_printf("%X", 255)` → `FF` |
| `%%` | Печать символа `%` | `ft_printf("%%")` → `%` |

### Bonus часть: Флаги

| Флаг | Описание | Пример |
|------|----------|---------|
| `-` | Выравнивание влево | `ft_printf("%-10d", 42)` → `42        ` |
| `0` | Заполнение нулями | `ft_printf("%010d", 42)` → `0000000042` |
| `.` | Точность | `ft_printf("%.5d", 42)` → `00042` |
| `#` | Альтернативная форма | `ft_printf("%#x", 42)` → `0x2a` |
| `+` | Знак для положительных | `ft_printf("%+d", 42)` → `+42` |
| ` ` | Пробел для положительных | `ft_printf("% d", 42)` → ` 42` |
| `width` | Минимальная ширина | `ft_printf("%10d", 42)` → `        42` |

#### Примеры комбинаций:
```c
ft_printf("%+010d", 42);      // "+000000042"
ft_printf("%-10.5d", 42);     // "00042     "
ft_printf("%#10x", 42);       // "      0x2a"
ft_printf("%.5s", "Hello");   // "Hello"
```

---

## 🏗️ Архитектура

### Выбор алгоритма и структуры данных

**Алгоритм**: Последовательный парсинг

**Обоснование**:
1. **Простота**: Один проход по строке формата
2. **Эффективность**: O(n) временная сложность
3. **Соответствие стандарту**: Порядок флагов в printf() фиксирован
4. **Модульность**: Каждый этап — отдельная функция

**Структура данных для флагов**:
```c
typedef struct s_flags
{
    int     minus;      // Флаг '-'
    int     zero;       // Флаг '0'
    int     width;      // Ширина поля
    int     precision;  // Точность (-1 = не указан)
    int     hash;       // Флаг '#'
    int     plus;       // Флаг '+'
    int     space;      // Флаг ' '
    char    specifier;  // 'c', 's', 'd', и т.д.
}   t_flags;
```

**Почему структура?**
- Все флаги в одном месте
- Легко передавать между функциями
- Понятная инициализация

**Алгоритм печати чисел**: Рекурсивный

**Преимущества**:
- Нет необходимости в буфере
- Экономия памяти
- Простая реализация для любого основания

---

## 📚 Ресурсы


### Документация

- [printf man page](https://man7.org/linux/man-pages/man3/printf.3.html) - официальная документация
- [Variadic Functions](https://en.cppreference.com/w/c/variadic) - вариадические функции
- [stdarg.h](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/stdarg.h.html) - работа с переменными аргументами



### Статьи и туториалы

- [Understanding printf internals](https://www.cypress.com/file/54761/download) - внутреннее устройство printf
- [Format specifiers guide](https://www.gnu.org/software/libc/manual/html_node/Output-Conversion-Syntax.html) - спецификаторы формата



### Инструменты тестирования

- [francinette](https://github.com/xicodomingues/francinette) - автоматическое тестирование
- [printfTester](https://github.com/Tripouille/printfTester) - расширенные тесты
- Valgrind - проверка утечек памяти


---

## 🤖 Использование AI

AI (Claude) использовался для следующих задач:


### Проверка корректности

- Верификация логики парсинга флагов
- Проверка обработки edge cases
- Валидация порядка печати элементов



### Оптимизация

- Предложения по улучшению производительности
- Рефакторинг повторяющегося кода
- Советы по структурированию



### Тестирование

- Генерация граничных условий
- Создание комплексных тестовых сценариев
- Сравнение с поведением стандартного printf



### Документирование

- Структурирование документации
- Создание примеров использования
- Форматирование README.md


**Важно**: Весь основной код написан самостоятельно. AI использовался исключительно как вспомогательный инструмент для улучшения качества кода и документации.

---

## ✅ Статус проекта


- [x] Обязательная часть реализована
- [x] Bonus часть реализована
- [x] Norminette проверка пройдена
- [x] Тесты с francinette: passed
- [x] Valgrind: нет утечек памяти
- [x] Документация завершена


---

## 🧪 Тестирование

### Запуск базовых тестов
```bash
make test
./test_printf
```

### Проверка утечек памяти
```bash
make bonus
valgrind --leak-check=full --show-leak-kinds=all ./test_printf
```

### Проверка нормы
```bash
make norm
```

---

## 📊 Производительность

| Операция | Сложность |
|----------|-----------|
| Парсинг формата | O(n) |
| Печать числа | O(log n) |
| Печать строки | O(n) |

---

## 🐛 Известные ограничения

1. **Нет буферизации**: Каждый символ пишется отдельным `write()` (по требованию задания)
2. **Нет поддержки float**: `%f`, `%e`, `%g` не реализованы
3. **Нет locale**: Всегда используется ASCII

---

## 👥 Авторы

- [Ваше имя] - [ваш логин@student.42.fr]

---

## 📝 Лицензия

Этот проект является частью учебной программы 42 и предназначен только для образовательных целей.

---

## 🙏 Благодарности


- 42 School за замечательную учебную программу
- Peer evaluators за конструктивную критику
- Open source сообществу за документацию и инструменты